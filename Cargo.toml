[workspace]

members = [
  "fpm-lib",
  "fpm-cli",
]

# generated by 'cargo dist init'
[profile.dist]
debug = true
inherits = "release"
split-debuginfo = "packed"

[workspace.metadata.release]
shared-version = true
verify = false
