[workspace]

members = [
  "lib_fpm",
  "fpm",
]

# generated by 'cargo dist init'
[profile.dist]
debug = true
inherits = "release"
split-debuginfo = "packed"

[workspace.metadata.release]
shared-version = true
